# Quote verification

Verified 58/65 (89%).

## Unverified

These quotes didn't match any of their listed source texts and were stripped from the corpus before authoring.

- **principles** / _Adversarial Training as Regularization_ (sources: src_015) — match ratio 0.69
  > it turns out that adversarial training is a regularizer... if you're overfitting then you will actually reduce the amount that you over fit you should do it anyway
- **principles** / _Linearity Causes Adversarial Vulnerability_ (sources: src_015) — match ratio 0.74
  > we might actually be seeing adversarial examples coming from the model being too linear... modern deep networks are very piecewise linear
- **principles** / _Non-Saturating Objectives for GANs_ (sources: src_017) — match ratio 0.56
  > Rather than training G to minimize log(1 − D(G(z))) we can train G to maximize log D(G(z)). This objective function... provides much stronger gradients early in learning.
- **frameworks** / _Machine Learning Algorithm Decomposition_ (sources: src_009) — match ratio 0.67
  > I tend to think of machine learning algorithms as decomposed into really three different pieces there's the model... the optimization algorithm... and then the last part is the data set
- **mental_models** / _Generative Models as Human Imagination_ (sources: src_009) — match ratio 0.80
  > When again creates a new image of a cat it's using a neural network to produce a cat that has not existed before... It's analogous to human imagination.
- **heuristics** / _Non-Saturating Gradient Trick_ (sources: src_007, src_039) — match ratio 0.74
  > Rather than training G to minimize log(1 − D(G(z))) we can train G to maximize log D(G(z)).
- **signature_quotes** / _Static Models as Sitting Ducks_ (sources: src_009) — match ratio 0.63
  > Right now we tend to train models and then after they're trained we freeze them... That's really a sitting duck from a security point of view.
