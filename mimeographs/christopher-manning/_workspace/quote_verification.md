# Quote verification

Verified 51/57 (89%).

## Unverified

These quotes didn't match any of their listed source texts and were stripped from the corpus before authoring.

- **principles** / _AI Should Complement Human Intelligence_ (sources: src_011) — match ratio 0.68
  > there's absolutely no reason why an artificial intelligence has to be trying to be exactly the same as human intelligence... we'd be much better aiming at complenting what humans are good at in our artificial intelligences.
- **principles** / _Meaning via Use_ (sources: src_013) — match ratio 0.74
  > inferentialism couches understanding as appreciating the appropriate versus inappropriate ways to use language and context so that's often summed up as a use theory of meaning... I think it fits much better with the models we're building in the llm world
- **frameworks** / _Bagel (Exploration-Guided Synthetic Demonstrations)_ (sources: src_013) — match ratio 0.72
  > we wanted to play around to produce synthetic demonstrations with an exploration policy that is a prompted llm... we use our large language models we use the second prompted large language Model A trajectory labeler that looks at a trajectory and tries to assign a natural language instruction
- **frameworks** / _Interactive Linguistic Web Agent Loop_ (sources: src_011) — match ratio 0.61
  > We're going to give it an action space... give it an objective... tell it a history of what's happened in the past and then we'll tell it about its current state... the accessibility tree that corresponds to a web page.
- **anti_patterns** / _Confusing Vast Knowledge with Intelligence_ (sources: src_011) — match ratio 0.73
  > By and large, they are good because they have enormous knowledge... but they're not actually very good at thinking about different circumstances that haven't been seen before.
- **anti_patterns** / _The 'Kaggle-ization' of ML Research_ (sources: src_016) — match ratio 0.74
  > It's simultaneously both the easier thing to do and the easier way to get short-term success in the machine learning community to say okay well here's this data set... and my only goal is to build a bigger deeper neural network that'll score a higher number.
