# Quote verification

Verified 92/112 (82%).

## Unverified

These quotes didn't match any of their listed source texts and were stripped from the corpus before authoring.

- **principles** / _Continuous Diffusion of AGI_ (sources: src_000, src_024) — match ratio 0.77
  > This assumption of suddenly having a completely intelligent, fully flexible, fully general human in a box... I don't think that we will have this discrete change. I think we'll arrive at the same kind of gradual diffusion of this across the industry.
- **principles** / _Agency Over Intelligence_ (sources: src_021) — match ratio 0.74
  > Agency > Intelligence I had this intuitively wrong for decades, I think due to a pervasive cultural veneration of intelligence... Agency is significantly more powerful and significantly more scarce.
- **principles** / _Build to Understand_ (sources: src_000) — match ratio 0.76
  > If I can't build it, I don't understand it... don't write blog posts, don't do slides, don't do any of that. Build the code, arrange it, get it to work.
- **principles** / _LLMs are Statistical Simulators, Not Magical AIs_ (sources: src_012) — match ratio 0.75
  > you're not talking to a magical AI you're talking to an average labeler... you're talking to kind of like an instantaneous simulation of that kind of a person that would be hired in the construction of these data sets
- **principles** / _Separate Algorithm from Efficiency_ (sources: src_039) — match ratio 0.75
  > This file is the complete algorithm. Everything else is just efficiency.
- **principles** / _Treat the Context Window as a Precious Resource_ (sources: src_017) — match ratio 0.66
  > think of the tokens in the context window as a precious resource... don't overload it with irrelevant information and keep it as short as you can
- **frameworks** / _The Autonomy Slider_ (sources: src_010, src_029, src_002, src_055) — match ratio 0.71
  > There's this what I call 'autonomy slider,' where more and more stuff is automated... and we're doing a bit less and less and raising ourselves in the layer of abstraction over the automation.
- **frameworks** / _Byte Pair Encoding (BPE) Tokenization_ (sources: src_012) — match ratio 0.47
  > we're basically looking for consecutive bytes or symbols that are very common... we're going to group this pair into a new symbol... each time when we mint a new symbol we're decreasing the length and we're increasing the symbol size
- **frameworks** / _Empirical Factuality Tuning_ (sources: src_012) — match ratio 0.48
  > we interrogate the model three times and we compare its answers... to the correct answer and if the model doesn't know... we create a new conversation in the training set... the answer is I'm sorry I don't know
- **frameworks** / _Few-Shot Prompt Construction_ (sources: src_017) — match ratio 0.76
  > I used kind of like an XML like language just so that the model understands that the example one begins here and ends here... I just give it a few examples and I kind of like specify them in detail
- **frameworks** / _LLM Pre-training Data Pipeline_ (sources: src_012) — match ratio 0.44
  > there's a ton of different types of websites that are just eliminated at this stage... the second part is text extraction... The next stage here is language filtering... finishing with for example the pii removal
- **frameworks** / _The LLM Council_ (sources: src_017) — match ratio 0.74
  > I kind of refer to all these models as my llm Council... If I'm trying to figure out where to go on a vacation I will ask all of them
- **frameworks** / _The Three Stages of LLM Training_ (sources: src_012) — match ratio 0.63
  > first what we do is we do pre-training... the second thing then is we go into the sft stage... and then finally in the last stage we do all the practice problems in the RL stage
- **mental_models** / _LLM Training as Human Education_ (sources: src_012) — match ratio 0.63
  > first what we do is we do pre-training... the second thing then is we go into the sft stage... and then finally in the last stage we do all the practice problems in the RL stage
- **mental_models** / _Lossy Compression of the Internet (The Zip File)_ (sources: src_012) — match ratio 0.75
  > you can think of the 405 billion parameters is kind of like a zip file but it's not a loss less compression it's a loss C compression
- **heuristics** / _Build to Understand_ (sources: src_000, src_044, src_029) — match ratio 0.71
  > If I can't build it, I don't understand it... I 100% have always believed this very strongly
- **heuristics** / _Let Students Guess_ (sources: src_000) — match ratio 0.69
  > Don't present the solution before giving the student a shot to guess it.
- **signature_quotes** / _Algorithm vs Efficiency_ (sources: src_039) — match ratio 0.75
  > This file is the complete algorithm. Everything else is just efficiency.
- **signature_quotes** / _Average Labeler Simulation_ (sources: src_012) — match ratio 0.63
  > you're not talking to a magical AI you're talking to an average labeler... an instantaneous simulation of that kind of a person
- **signature_quotes** / _Build to Understand_ (sources: src_000) — match ratio 0.76
  > If I can't build it, I don't understand it... don't write blog posts, don't do slides, don't do any of that. Build the code, arrange it, get it to work.
