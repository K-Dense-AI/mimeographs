[project]
name = "mimeographs"
version = "0.1.0"
description = "A collection of ready-to-use expert skills (SKILL.md + AGENTS.md) that clone the thinking of specific people into your agent."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
